class TX:
    cmd="TX"
    description = "TX mode"
    # desc, return, format, example response
    getExample= []
    # desc, format, example response
    setExample= ["Set radio in TX mode depending on selected band", "TX", "TX 0"]
