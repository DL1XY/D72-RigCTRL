class RX:
    cmd="RX"
    description = "RX mode"
    # desc, return, format, example response
    getExample= []
    # desc, format, example response
    setExample= ["Set radio in RX mode", "RX", "RX"]
